{
  "devDependencies": {
    "abrusco": "^0.9.0"
  },
  "scripts": {
    "w": "npm run build:css -- --watch",
    "build:css": "abrusco src/css --minify -o static/master.css",
    "build": "npm run build:css"
  }
}
